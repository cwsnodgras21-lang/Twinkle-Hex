/**
 * Shopify Admin API integration placeholder.
 * Use for: orders, customers, inventory, products.
 * Requires: SHOPIFY_ADMIN_ACCESS_TOKEN, SHOPIFY_STORE_DOMAIN
 *
 * Future: Shopify Admin REST or GraphQL client.
 * Future: Sync orders → local DB for reporting.
 * Future: Pull customer data for retail/event tracking.
 * Future: Inventory reconciliation with finished_inventory.
 */

// Placeholder - no implementation yet
export const shopifyAdmin = {
  // Orders - where Shopify order data plugs in
  // fetchOrders: async (params) => { ... },
  // getOrderById: async (id) => { ... },

  // Customers - where Shopify customer data plugs in
  // fetchCustomers: async (params) => { ... },
  // getCustomerById: async (id) => { ... },

  // Products - where Shopify product data plugs in
  // fetchProducts: async (params) => { ... },
  // getProductByHandle: async (handle) => { ... },

  // Inventory - where Shopify inventory levels plug in
  // fetchInventoryLevels: async (params) => { ... },
  // updateInventory: async (params) => { ... },
};
