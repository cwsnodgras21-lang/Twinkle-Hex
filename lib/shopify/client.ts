/**
 * Shopify integration layer placeholder.
 * Future: Shopify Storefront API client.
 * Future: Product fetch, cart, checkout.
 * Future: Use env vars: NEXT_PUBLIC_SHOPIFY_STORE_DOMAIN, SHOPIFY_STOREFRONT_ACCESS_TOKEN
 */

// Placeholder - no mock behavior, just structure for future implementation
export const shopify = {
  // Future: fetchProducts, fetchProductByHandle, createCart, etc.
};
