# Styles

Shared styles and design tokens live here.

- `globals.css` is in `app/globals.css`
- Brand palette is in `tailwind.config.ts` and CSS variables in `globals.css`
- Future: Component-specific CSS modules if needed
- Future: Animation keyframes, utility classes
